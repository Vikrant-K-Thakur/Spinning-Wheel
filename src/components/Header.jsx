import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      <h1 className="title">Alphabet Spinning Wheel</h1>
      <p className="subtitle">Click the SPIN button to land on a random letter!</p>
    </header>
  );
};

export default Header;